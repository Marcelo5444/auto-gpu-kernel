# Experiment summary

| Exp | Date | Description | Latency | Ref | Pass | Notes |
|---|---|---|---|---|---|---|
| 1 | 2026-04-16 | Fused Triton, online softmax, BLOCK_N=64, one-prog-per-token | 0.100 ms | — | 12/12 | new best (first Triton impl; pre-scale Q→bf16 caused err, fixed by post-scale in fp32) |
| 2 | 2026-04-16 | Split-K (NUM_SPLITS=8) flash-decoding + combine kernel | 0.024 ms | — | 12/12 | new best, **−76% / 4.2×** (needed NaN guard for all-padding splits) |
| 3 | 2026-04-16 | Early-exit `if max(idx)>=0` around compute (NUM_SPLITS=8) | 0.029 ms | — | 12/12 | mixed: small −33% / large +25%; `if` defeats num_stages pipelining → regression on median; **revert** |
| 4 | 2026-04-16 | NUM_SPLITS=16 + dynamic combine loop + num_stages=2 | 0.036 ms | — | 12/12 | regression across the board (+33-50%); combine cost grows linearly with splits, sweet spot stays at 8; **revert** |
| 5 | 2026-04-16 | Ablation: combine change alone (dynamic range + num_stages=2, NUM_SPLITS=8) | 0.030 ms | — | 12/12 | A/B vs exp_2: 12/12 A wins, +22% regression; `static_range(8)` + `num_stages=1` is optimal for combine; **revert** |
| 6 | 2026-04-16 | Dynamic loop bound early-exit (scan indices once, `range(0, num_blocks*BLOCK_N)`) | 0.022 ms | — | 12/12 | **new best, A/B 11/12 wins** vs exp_2, mean −10% / small workloads −29 to −42% / large neutral. Exploits 88% padding while preserving `num_stages=2` pipelining |
| 7 | 2026-04-16 | D_ckv-parallel combine (grid `(T, 4)`, each CTA = 128 channels) | 0.021 ms | — | 12/12 | **new best, A/B 12/12 wins** vs exp_6, mean −6%; combine phase 3× memcpy-floor unblocked; m/l state replication across 4 CTAs is negligible |
| 8 | 2026-04-16 | Tune D_CKV_SPLIT=8 (BLOCK_D=64) vs 4/16 | 0.020 ms | — | 12/12 | **new best, A/B 10/12 wins** vs exp_7 at D=8, mean −6%; D=16 is a wash; D=8 is sweet spot |
| 9 | 2026-04-16 | BLOCK_N=128 (double from 64) | 0.016 ms | — | 12/12 | **new best, A/B 9/12 wins** vs exp_8, mean −15%. Big wins on T≥6 (−17 to −35%), regresses T=1/some T=2 (+13-18%) due to wasted compute; net positive on median |
| 10 | 2026-04-17 | Fused single-launch D-parallel kernel (no combine, no partial_acc) | 0.033 ms (median) | — | 12/12 | **ablation/regression**. Bimodal: T≤2 wins 36–46% (launch tax eliminated), T≥6 regresses 2–3.5× (softmax + Q@K^T replicated 8× across D-programs). Hybrid dispatch → exp_11 |
| 11 | 2026-04-17 | Hybrid dispatch: fused for T≤2, split+combine for T≥3 | 0.016 ms (median) | — | 12/12 | **new best, A/B 12/12 wins** vs exp_9, mean −12% (−0.0022 ms). T=1/T=2 −44 to −46%; T≥6 matches exp_9. Small-T absolute latency drops from 0.011 → 0.007 ms |
| 12 | 2026-04-17 | bf16 partial_acc (halves HBM byte volume) | — | — | 12/12 | **regression**. A/B 6/12 wins vs exp_11, mean +0.0004 ms. Large T regresses 3–10% — partial_acc (≤2 MB) already L2-resident between split+combine; bf16 cast adds overhead without HBM saving. Reverted |
| 13 | 2026-04-17 | BLOCK_N_FUSED=64 (halve fused path's block size; split stays 128) | 0.016 ms (median) | — | 23/23 | **new best, A/B 9/12 wins** vs exp_11, mean −0.0008 ms (−5.3%). T≤2 low-valid wins −26% (0.007→0.005 ms). One T=2 high-valid outlier (05f6de65) regresses +23%; net still clearly positive. Loop-iter vs HBM tradeoff crossover at ~100-500 valid |
| 14 | 2026-04-17 | num_warps=4 on fused (down from 8) with BLOCK_N_FUSED=64 | — | — | 12/12 | **regression**. A/B 3/12 wins vs exp_13 (all ties on unchanged paths), mean +0.0002 ms. All 5 fused-path workloads regress 3–5%. 8 warps × 1 head = ideal softmax reduction mapping; 4 warps forces cross-head serialization. Reverted |
| 15 | 2026-04-17 | Fused split+combine via atomic barrier (large-T path; T≤2 fused kernel unchanged) | 0.016 ms (median) | — | 23/23 | **new best, A/B 10/12 wins** vs exp_13, mean −0.0011 ms (−6.7%). Large T wins −8 to −12% (0.017→0.016 ms); small T unchanged. `atomic_add(+1, release)` + spin `atomic_add(0, acquire)` + end-of-kernel `atomic_add(-1)` reset pattern. Module-level counter cache avoids `torch.zeros` per-call. Saved ~2 µs of ~8 µs projected launch-tax |
| 16 | 2026-04-17 | Vectorized combine: load all 8 partials at once + 3D reduction (replace `tl.static_range`) | 0.019 ms (T=8) | — | 2/2 | **regression**. +19% on T=8. `tl.static_range(8)` unrolled chain outperforms 3D-load + `tl.sum(axis=0)` — unrolled compile flows better; 3D reduction adds shmem/reg pressure. Reverted |
| 17 | 2026-04-17 | num_warps=16 on fused_split_combine_kernel (keep fused_attn at 8) | 0.018 ms (T=8) | — | 2/2 | **regression**. +12.5% on T=8. num_warps=8 strict optimum for H=16 (matches exp_14's 4-warps regression): 16 warps force 1-head-per-warp mapping that wastes MMA tile rows. Reverted |
| 18 | 2026-04-17 | Spin-poll: swap `atomic_add(0, acquire)` → `tl.load(volatile=True)` + `tl.debug_barrier()` post-spin | 0.016 ms (median) | — | 23/23 | **new best, A/B 9–10/12 wins** vs exp_15 (two runs), mean −0.0001 ms (−0.6%). Large T consistently −0.44 to −1.40%. RMW poll was serialising through L2 atomic unit; volatile load is pure L2 read, no contention. Release/decrement atomics unchanged |
| 19 | 2026-04-17 | num_stages=3 on fused_split_combine_kernel (keep num_warps=8) | — | — | 12/12 | **regression**. A/B 3/12 wins vs exp_18, mean +0.0005 ms. All 7 large-T workloads regress +5.78 to +6.44%. Split loop runs 1-2 iters max (SPLIT_SIZE=256, BLOCK_N=128); num_stages=3 allocates 3× K-tile shmem (384 KB > B200's 228 KB/SM) → register spills or reduced occupancy with no compute benefit. Reverted |
| 20 | 2026-04-17 | Action 1: cluster launch `num_ctas=8` on large-T kernel — COMPILE FAIL. Action 2 fallback: reorder idx_scan before Q in small-T kernel | — | — | 12/12 | **both reverted**. Action 1 hit `TritonGPUPlanCTAPass` assertion (atomic-barrier incompatible with Triton 3.6 CTA planner). Action 2 A/B 8/12 wins but mean ≈ 0 (noise); wins concentrated on *unchanged* large-T kernel → VM noise, not signal. Small-T workloads (where change applies) slightly regress. Reverted both |
| 21 | 2026-04-17 | `launch_pdl=True` on both kernels (Programmatic Dependent Launch) | — | — | 12/12 | **regression / noise**. A/B 4/12 wins vs exp_18, mean +0.0000 ms. PDL requires upstream kernel to emit `griddepcontrol.launch_dependents` PTX; Triton doesn't auto-insert this. Single-flag PDL is a no-op without that signal. All deltas <1%. Reverted. Triton scalar kwargs now fully exhausted |
| 22 | 2026-04-17 | Fresh-start Gluon rewrite of T≥3 split path (`gl.dot_fma`, explicit BlockedLayouts); T≤2 Triton path unchanged. Split+combine = two launches (no atomic barrier) | 1.30 ms (large-T median) | — | 12/12 | **correctness bring-up, not a new best**. ~65–85× slower than exp_18 on large T — `gl.dot_fma` is software FMA (no tensor cores). 18 Gluon probes done → durable API notes on `DotOperandLayout(k_width=0)`, dtype-match rule, warps_per_cta replication, layout conversions, `gl.barrier` unavailability in JIT. Foundation for exp_23+: replace `dot_fma` with `bw.tcgen05_mma`, re-fuse with `bw.mbarrier`, then `num_ctas=NUM_SPLITS` clusters. Kept for follow-up; revert if Gluon can't beat exp_18 within a few iterations |
| 23 | 2026-04-17 | **Revert Gluon → exp_18 baseline**; ablation: `cache_modifier=".cg"` (bypass L1) on split-kernel K_ckv/K_pe loads only (fused kernel reverted — regressed small-T in first A/B) | 0.016 ms (large-T median) | exp_18 | 12/12 | **marginal, kept**. A/B 8/12 B wins vs exp_18, mean Δ ≈ 0.0000 ms. Large-T 6/7 B wins, Δ ∈ [-0.18%, -0.02%] (consistent but below noise floor). `.cs` modifier errored at compile (not supported on Triton 3.6). `.cg` zero-cost; kept for tiny direction-positive signal on T≥6 |
| 24 | 2026-04-17 | `cache_modifier=".cg"` on `partial_m`/`partial_l`/`partial_acc` STORES in split phase (bypass L1 → direct L2) | 0.016 ms (large-T median) | exp_23 | 12/12 | **marginal, kept**. A/B 7/12 B wins, mean Δ ≈ 0. Large-T 7/7 B wins, Δ ∈ [-0.38%, -0.02%] (consistent but still below noise floor). Confirms `.cg` is supported on `tl.store` in Triton 3.6. Zero-cost, directionally positive — kept. Cache tuning levers saturating; next exp should pivot |
| 25 | 2026-04-17 | Close PDL loop: `tl.inline_asm_elementwise("griddepcontrol.launch_dependents;", ...)` epilogue on BOTH kernels + `launch_pdl=True` on both launches | 0.016 ms (large-T median) | exp_24 | 2/2 | **reverted — neutral/invisible**. A/B 6/12 wins, mean Δ ≈ 0. PTX + `launch_pdl` both compile and execute cleanly on sm_100a. But CUPTI measures individual kernel duration; PDL's cross-kernel prologue overlap is *structurally invisible* to the harness. Extends LESSON-29: PDL cannot be pursued under current measurement methodology. Pivot to structural changes |
| 26 | 2026-04-17 | **Stride-partition TopK**: split `s` owns positions `{s, s+NUM_SPLITS, ...}` instead of contiguous block — distributes prefix-valid run across all 8 split CTAs | 0.016 ms (large-T median); **0.012 ms on 4c46a94b T=6, 0.011 ms on 68d6817d** | exp_24 | 23/23 | **new best, kept**. A/B two VMs both show mean Δ = −0.0003 ms favor B (3/12 and 7/12 wins). Reproducible −25 to −27% on 4c46a94b (T=6) across 3 measurements. 4 large-T T=8 workloads slight +0.5–1.8% regression (HBM coalescing trade-off on strided K-gather). Net win driven by the two outliers. Cleanest structural win since exp_15 |
| 27 | 2026-04-17 | Compact-block partition by valid-count: scan full 2048, split `s` owns `[s*nv/NUM_SPLITS, (s+1)*nv/NUM_SPLITS)` valid positions. Goal: preserve exp_26 load balance + restore HBM coalescing | — | exp_26 | **1/2 on quick (T=8 FAILED, abs_err=2.82)** | **correctness failure — reverted**. T=1 passed; T=8 (high-valid) workload output corrupted. Root cause: likely `(s * num_valid_total) // NUM_SPLITS` scalar-tensor arithmetic edge case or full-TopK scan artifact. Deferred debugging; pivot to different axis |
| 28 | 2026-04-17 | `cache_modifier=".cg"` on partial_m/l/acc LOADS in combine phase (complement exp_24's `.cg` on stores) | — | exp_26 | 2/2 | **regression — reverted**. A/B 2/12 wins vs exp_26, mean Δ = +0.0001 ms (A faster). 4c46a94b +3.22% (worst; the stride-win workload). `.cg` on loads marginally penalizes a path where L1 tag check was already overlapping with MMA compute. Cache-modifier axis saturated — pivot required |
| 29 | 2026-04-17 | Retry compact-block partition (exp_27) **with `in_range` mask** on idx load to fix overshoot bug | — | exp_26 | 2/2 (correctness fixed) | **large regression — reverted**. A/B 2/12 wins, mean Δ = +0.0009 ms (+10% large-T). Bug fix worked but full-TopK pre-scan (2048 int32 load + reduction) costs more than coalescing saves. Compact-block is theoretically sound but infeasible without a separate pre-scan kernel. Axis closed |
| 30 | 2026-04-17 | Research-agent plan: cache `partial_m/l/acc` scratch buffers across calls (extend `_counter_cache` pattern) | — | exp_26 | 2/2 | **reverted — neutral/mild regression**. A/B 3/12 wins, mean Δ ≈ 0. 4c46a94b +1.94% (Python helper overhead). Falsifies research plan's hypothesis that CUPTI sees `torch.empty` cost — caching allocator masks it. `_counter_cache` only helps because LESSON-22's ~5 µs was `cudaMemset` zero-init, not allocation. Host-overhead axis now fully closed |
| 31 | 2026-04-17 | `NUM_SPLITS=4` (down from 8) + `BLOCK_D=128`. Re-tune combine under stride-partition | 0.026 ms (T=8) | exp_26 | 2/2 | **large regression — reverted**. T=8 +62% (0.016 → 0.026 ms). Under stride, each split's iter-count scales ∝ 1/NUM_SPLITS → split critical path doubles. Combine halving (~6% save) can't offset split doubling. NUM_SPLITS=8 is strict optimum across both block (exp_4) and stride schemes |
| 32 | 2026-04-17 | `num_stages=1` on split kernel (down from 2). Hypothesis: mostly 1-iter loops under stride | 0.026 ms (T=8) | exp_26 | 2/2 | **large regression — reverted**. T=8 +62% (0.016→0.026 ms). Killing double-buffering serializes K-load with MMA. Hypothesis falsified: T=8 workloads hit 2 iters under stride, so pipelining is live. Sweet spot at num_stages=2 confirmed across full sweep (exp_19 tested =3, regress; exp_32 tested =1, regress) |
| 33 | 2026-04-17 | `BLOCK_N=256` on split kernel (double from 128). Removes loop overhead (max 1 iter under SPLIT_SIZE=256) | — | exp_26 | COMPILE FAIL | **OOM shmem — reverted**. Required 321 KB > B200's 232 KB/SM. BLOCK_N × D × 2 × num_stages = 256×576×2×2 = 576 KB raw + overhead. num_stages=1 would fit but regresses T=8 (exp_32). BLOCK_N ↔ num_stages coupling = hard wall in Triton 3.6 |
| 34 | 2026-04-17 | `num_stages=3` on `_fused_attn_kernel` (small-T T≤2 path). Fused iterates up to 32× so deeper pipelining may help | — | exp_26 | 12/12 | **regression — reverted**. A/B 6/12 ties on unchanged paths, all 5 fused-path workloads regress +5-10% (T=1 +10%, T=2 +5-9%). Deeper pipelining over-provisions shmem when loops are short (low-valid → 1 iter); occupancy/spill hit. num_stages=2 confirmed strict optimum on both kernels. num_stages axis fully closed |
| 35 | 2026-04-17 | Gluon pivot plan (stage 1: `bw.tcgen05_mma`) — research agent plan marked **abandoned** (plateau only 8 reverts deep, plan self-admits investment-regression conflicts with per-iter revert rule). Preserved for later | — | — | ABANDONED | plan-only; work moved to exp_36 for an untested Triton-side axis |
| 36 | 2026-04-17 | `D_CKV_SPLIT_FUSED=1` on `_fused_attn_kernel` (T≤2 path); constexpr branch reuses `kc` when BLOCK_D==D_CKV | 0.022 ms (T=2 worst) | exp_26 | 12/12 | **regression — reverted**. A/B 4/12 (large-T ties), all 5 fused-path workloads regress **+17-22%** (T=1 +21%, T=2 +17-19%). 1-CTA-per-token loses HBM latency hiding + parallel D-output stores that 8-way D-parallelism provides. LESSON-16's "replicated Q@K^T" is L2-cached (nearly free); D-parallelism is load-bearing. D_CKV_SPLIT_FUSED axis closed at 8 |
| 37 | 2026-04-17 | **Monotonic counter**: skip kernel-end `atomic_add(-1)`; host-side `target_count = gen * NUM_SPLITS`; spin checks `count < target_count` | 0.016 ms (large-T median) | exp_26 | 23/23 | **new best, kept**. A/B 10/12 B wins vs exp_26, mean Δ = −0.0003 ms. **All 7 T≥3 workloads improve −2.5 to −3.6%** (mean −2.88%, ~0.44 µs saved per call). T≤2 fused-path neutral (no counter). First non-revert since exp_26; removes actual work (the decrement was pure bookkeeping) rather than trading costs |
| 38 | 2026-04-17 | Per-slot ready vector: `atomic_add(Counter_ptr + t*NUM_SPLITS + s, 1)` (8 slots same cache line) + vectorized 8-wide spin-sum | — | exp_37 | 12/12 | **regression — reverted**. A/B run 1: 3/12 B wins, mean +0.0000. Run 2: 0/12, mean +0.0001 (+0.5-1.2%). Same-cache-line atomics don't parallelize on B200 L2 atomic unit; vectorized spin load + `tl.sum` adds ~3-5 ns per poll iter × ~70 iters = ~250 ns regression. Atomic+barrier lever now fully closed |
| 39 | 2026-04-17 | Merge partial_m + partial_l → partial_ml `[T,S,H,2]` interleaved; `tl.join`/`tl.split` hot path | — | exp_37 | 12/12 | **regression — reverted**. A/B run 1: 3/12, run 2: 0/12, mean +0.0001 ms. +0.5-1.5% across T≥3 (232ed014 +1.53%, 2207f0fd +1.32%). Plan's 0.05-0.15 µs projection optimistic: partial_* already L2-resident (<4 KB each), join/split register shuffle exceeds cache-locality gain. Combine-IO axis now fully closed (exp_28/30/39 triple fail) — triggers Gluon pivot per plan coordination note |
| 40 | 2026-04-17 | **Gluon stage 1**: port exp_22 scaffold + replace `gl.dot_fma` → `bw.tcgen05_mma`. Probe-first (`scripts/probe_gluon19.py`). Hybrid dispatch preserved: T≤2 Triton unchanged, T≥3 Gluon two-launch | 0.095 ms (T≥3) | exp_37 | 12/12 | **regression — reverted**. Correctness passes (abs_err 5.3e-5 on probe, 12/12 on stride-2). Latency 6.1× slower than exp_37 (gate was 0.025 ms, exceeded 3.8×). Root cause: HM=128 MMA tile forces D-chunking (5 MMAs/iter × 4 iters/split × 8 splits = 160 MMA-fences ≈ 80 µs) + SMEM pressure requires per-D-chunk Q_nope HBM reload (~16 µs) + 2-launch tax (~8 µs). Stage 2 (mbarrier) saves only ~8 µs; stage 3 (cluster) adds ~17 µs more — combined best case end-of-stage-3 = 0.070 ms, still 4.4× exp_37. **Gluon axis closed as a primary path.** LESSON-46. Triggers fresh research-agent synthesis for exp_41 |
| 41 | 2026-04-17 | Diagnostic: re-run `scripts/profile_kernel5.py` (overwrite 22-exp-stale profile.md) + apply `cache_modifier=".cg"` to Q_nope/Q_pe loads in `_fused_split_combine_kernel` (last unexplored cache-modifier axis) | 0.016 ms (large-T median) | exp_37 | 12/12 | **tie — reverted**. A/B 3/12 B wins vs exp_37, mean Δ=+0.0000 ms. Profile refresh: barrier bucket 2.23→0.71 µs (exp_37 monotonic recovered 68%); split_work=8.05 µs still dominant; no new bucket revealed. Triton ceiling ≈14.5 µs CUPTI vs current ~15.5 µs (within ~1 µs of floor). **Cache-modifier axis fully closed** across all 4 sub-axes (K loads exp_23, partial stores exp_24, combine loads exp_28, Q loads exp_41). LESSON-47 |
| 42 | 2026-04-17 | **Per-slot atomics at 128-byte stride** (LESSON-45's explicitly-skipped variant). Counter `[T]`→`[T×NUM_SPLITS×32]` (one 128-B cache line per slot). Each split CTA atomic-adds its own line; combine spins on `tl.min(slot_vec) >= gen` | — | exp_37 | 12/12 | **regression — reverted**. A/B 1/12 B wins vs exp_37, mean Δ=+0.0001 ms. Consistent +0.9–1.4% on all 7 T≥3 workloads. Either L2 atomic unit has <8 lanes at 128-B granularity or the 8-wide vectorized spin-load + `tl.min` reduction costs more than the producer-side parallelism saves. **Atomic-barrier axis fully closed** at both 32-B (exp_38) and 128-B (exp_42) slot stride; only unexplored reduction is cluster-sync (structurally blocked by LESSON-27) |
| 43 | 2026-04-17 | `eviction_policy="evict_first"` on `kc`/`kp` `tl.load` in `_fused_split_combine_kernel` (lines 98-99). Genuinely untested axis — orthogonal to `cache_modifier=".cg"` (L1 bypass); this is an L2 replacement hint. Under stride-partition (LESSON-42) K rows are one-shot per iter; evict-first frees L2 for multi-use Q_nope + partial_m/l/acc | 0.016 ms (large-T median) | exp_37 | 12/12 | **marginal keep — gate passed**. A/B 7/12 B wins vs exp_37, mean Δ≈0; 4/7 affected (T≥3) workloads direction-positive, sub-0.5% magnitudes. Matches exp_23/24 "keep because zero-cost and consistent direction" pattern. **Plateau broken** after 5 consecutive reverts (exp_38/39/40/41/42). LESSON-48 |
| 44 | 2026-04-17 | Extend `eviction_policy="evict_first"` to both `idx_scan` (line 86) and per-iter `idx` (line 92) loads in `_fused_split_combine_kernel`. Hypothesis: same one-shot pattern as K loads | — | exp_43 | 12/12 | **regression — reverted**. A/B 3/12 B wins vs exp_43, mean Δ=+0.0003 ms. Consistent **+3.3–3.8% on 6/7 T≥3 workloads**. Root cause: `idx_scan` and per-iter `idx` read the SAME underlying data (scan covers 256 indices, loop consumes 128 at a time from that range) — evict_first on scan caused per-iter refetch to miss L2 under KV-gather pressure (~8 µs HBM refetch penalty per call). Refinement: `evict_first` only valid for TRULY one-shot loads. LESSON-48 extended |
| 45 | 2026-04-17 | `eviction_policy="evict_first"` on per-iter `idx` (line 92) ONLY, keeping `idx_scan` (line 86) un-hinted. Hypothesis: per-iter IS last read of each idx row (KV gather fires next; idx untouched rest of combine) | — | exp_43 | 12/12 | **small regression — reverted**. A/B 1/12 B wins vs exp_43, mean Δ=+0.0001 ms. Consistent +0.6–1.1% on 6/7 T≥3 workloads. Mechanism: per-iter idx load is 512 B (128 × int32 = 4 L2 lines) — too small for eviction hint to free meaningful L2 capacity; hint's instruction-decode cost + possible prefetcher disruption outweighs benefit. LESSON-48 refined: evict_first needs (a) truly one-shot AND (b) freed capacity ≥ tens of KB |
| 46 | 2026-04-17 | `eviction_policy="evict_last"` on all three partial STORES (lines 124-126, partial_m/l/acc with existing `.cg` modifier) to keep lines L2-resident for combine readers across atomic barrier | — | exp_43 | 0/2 | **PTXAS compile error — reverted**. `Modifier '.evict_last' cannot be combined with modifier '.cg'`. Triton's eviction_policy maps to PTX L1-eviction hint; `.cg` bypasses L1, making the combination ill-defined. Store-side L2 eviction hints are not programmable on this path. Axis structurally blocked. LESSON-49 |
| 47 | 2026-04-17 | `input_precision="ieee"` on 3 `tl.dot` calls in `_fused_split_combine_kernel` (lines 101, 102, 112: Q_nope@K_ckv^T, Q_pe@K_pe^T, P@K_ckv). Genuinely untested precision axis | — | exp_43 | 12/12 | **tied no-op — reverted**. Two A/B runs: 8/12 then 7/12 B wins, mean Δ≈0 both. abs_err byte-for-byte identical (1.56e-02). `ieee` on bf16×bf16 lowers to same `wgmma.bf16.bf16.f32` as default; Triton's input_precision only selects among FP32 MMA strategies, not bf16. Axis closed for this kernel. Marginal-keep criterion requires mechanism, not just statistics (62.5% at sub-0.3% = noise without predictive mechanism). LESSON-50 |
| 48 | 2026-04-17 | `eviction_policy="evict_last"` on Q_nope (16 KB) and Q_pe (2 KB) loads in `_fused_split_combine_kernel` (lines 74-75). Cross-CTA L2 sharing: 8 D-parallel split CTAs for same token load identical Q tiles; `evict_last` pins lines for later CTAs | 0.016 ms (T=8 median) | exp_43 | 128/128 | **marginal keep — new best**. A/B run 1: 11/12 B wins (VM bias). A/B run 2: 6/12. **Stratified by affected path: T≥3 (combine kernel) 13/14 B wins across both runs = 92.9%, mean Δ ≈ −0.35%**. T≤2 (fused kernel unchanged) shows pure run-to-run noise. Mechanism clean: 8-way multi-reader cross-CTA fan-out × 18 KB × 8 CTAs = 144 KB L2 retained. abs_err byte-for-byte identical. Plateau broken after 4 consecutive non-wins (exp_44/45/46/47) |
| 49 | 2026-04-17 | **Research-agent plan**: `BLOCK_N = 128 → 64` on split-combine host dispatch (line 314). Hypothesis: workload p50 valid=33 → per-split valid≈4 wastes 97% of BLOCK_N=128; halving matches median footprint | 0.019 ms (T=7) | exp_48 | 12/12 | **massive regression — reverted**. A/B 2/12 B wins vs exp_48, mean Δ=+0.0016 ms. **6/7 T≥3 workloads regress +12-23%** (T=8 all at +22%); only 232ed014 at +1.4%. T≤2 unchanged. Plan's premise (per-split valid follows p50=4) wrong for THIS stride-2 workload set: T=8 benchmark tokens have per-token valid ≥512 → per-split ≥64 → iter count transitions 2→3 costing ~50% of split_work budget. LESSON-52: workload_profile.md percentiles don't generalize to stride-N-dispatched benchmark workloads |
| 50 | 2026-04-17 | Mirror exp_48 on fused path: `eviction_policy="evict_last"` on Q_nope/Q_pe in `_fused_attn_kernel` (lines 230-231, T≤2 path). Hypothesis: pin 18 KB Q across 32-iter loop so repeated Q@K MMAs avoid L2 refetch | — | exp_48 | 128/128 | **net-neutral across sessions — reverted**. Prior-session A/B: T=2 wins 8/8 / T=1 regresses 2/2. Fresh re-runs (different VM): T=1 variant-favors 2/3 by 1.5-2%, opposite direction. Absolute deltas 0.0001 ms on 5 µs workloads — below noise floor. Cross-session sign flip = pure noise, no real signal. Fused-kernel Q-eviction axis closed (combine-kernel evict_last in exp_48 still kept; different mechanism: 8-way cross-CTA fan-out on 0.015 ms workloads is above noise floor). LESSON-53: marginal keeps on small-T fused-kernel workloads require absolute delta ≥0.0002 ms AND same-direction across same-session A/B runs |
| 51 | 2026-04-17 | **Research-agent plan**: `NUM_SPLITS=16, D_CKV_SPLIT=16, BLOCK_D=32` (host dispatch lines 316-318). Hypothesis: T=7/8 workloads have per-token valid ≥1032 → 2-iter split path at NUM_SPLITS=8,BLOCK_N=128 → doubling splits drops to 1-iter | 0.012 ms (median large-T) | exp_48 | 23/23 | **NEW BEST — kept**. A/B stride-2 × 2 runs: mean Δ = -0.0012 ms, -6-8% median. **T=7/8 cluster 6/6 workloads improve -15.8 to -19.5% consistently across both runs** (~2.7 µs absolute each). One regression: 4c46a94b +14-15% (low-valid T, already 1-iter at NUM_SPLITS=8). Net: 16.2 µs saved vs 1.6 µs lost = 10:1 win/loss ratio. **Breaks "~1 µs from HBM floor" projection**: profile's floor was at fixed NUM_SPLITS=8 byte volume, doubling splits changes per-CTA bytes. LESSON-54. Follow-up (exp_52): adaptive NUM_SPLITS per-token to recover 4c46a94b |
| 52 | 2026-04-17 | Combine-loop `tl.static_range(NUM_SPLITS)` → `range(NUM_SPLITS)` on line 148 of `_fused_split_combine_kernel`. Hypothesis: at NUM_SPLITS=16 × BLOCK_D=32 = 32 KB unroll, LESSON-13 threshold hit; dynamic range should reduce icache pressure + enable auto-pipelining | — | exp_51 | 2/2 | **regression — reverted**. A/B stride-2: **7/7 T≥3 workloads regress +5-7%** (+0.7-0.9 µs combine slowdown). T=8 max-valid 05f6de65 tied. T≤2 noise. Mechanism: static unroll enables 16-wide ILP across softmax-rescale chain that auto-pipelining can't match (num_stages prefetches 1-2 iters ahead vs static's 16-wide). LESSON-13 refined: 32 KB unroll is the UPPER bound of static_range efficacy, not crossover — below 32 KB, static_range remains preferred even at 16 iters. Combine-loop unroll axis fully closed. LESSON-55 |
| 53 | 2026-04-17 | Combine tree-reduction — 3 variants: (A) `tl.reduce` with 3D-broadcast m/l, (B) offline softmax `tl.max`+scale+`tl.sum`, (C) two-pass pre-compute m_global then offline-scaled serial. Per profile.md "2-stage tree" recommendation + LESSON-55 serial rescale chain | — | exp_51 | 2/2 each | **all regress — reverted**. Variant A: +34-37% (3D broadcast 32× inflates per-step m/l work, `tl.reduce` serializes internally). Variant B: +10% (32-KB `pacc * scale` materialization defeats 2-KB rolling tile). Variant C: +20-25% (lost online-softmax ILP, still serial l_global/acc_comb write-back). Online-softmax + static_range compiler-optimized beyond semantic reformulation. Profile.md's "2 µs from tree" optimistic — critical path is already near-minimum. Combine-loop axis fully closed after 4 attempts (exp_4, exp_5, exp_52, exp_53). LESSON-56 |
| 54 | 2026-04-17 | **Research-agent plan**: adaptive NUM_SPLITS dispatch to recover 4c46a94b regression. Two variants: (A) T-based `num_tokens >= 7 → NS=16 else NS=8`; (B) `valid_max > 1024` via `.item()` sync | — | exp_51 | A 23/23, B 2/2 | **both reverted**. A: stride-2 A/B × 2 runs mean Δ = -0.0000 ms, neutral. Plan premise WRONG — workload_profile.md shows 4c46a94b is T=8, valid_max=1002 (not T=6), so T-rule can't target it. B: `.item()` sync blew 2207f0fd from 0.013 ms → 0.103 ms (**9× regression**), confirms LESSON-20 "host sync costs more than saving". Adaptive-dispatch axis closed; 4c46a94b regression (~1.6 µs / call) accepted as bounded cost. LESSON-57 |
